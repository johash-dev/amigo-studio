# /switch

Run `npx amigo switch <name>` with the project name from the user message (`none` or `studio` leaves the product drawer). If no name was given, ask once. Translate into Owner chrome for the new Where. Do not paste TOON. Only switch unless they asked to implement something.
